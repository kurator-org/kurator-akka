imports:
- classpath:/org/kurator/akka/types.yaml

types:

- id: Multiplier
  type: PythonFunctionActor
  properties:
    defaults:
      path: multiplier.py
      function: multiply

- id: NumReader
  type: PythonFunctionActor
  properties:
    trigger: true
    defaults:
      path: reader.py
      function: readnum

- id: NumPrinter
  type: PythonFunctionActor
  properties:
    defaults:
      path: printer.py
      function: printnum
